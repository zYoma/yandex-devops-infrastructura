apiVersion: v1
kind: Service
metadata:
  name: {{ include "sausage-store-chart.fullname" . }}
  labels:
    {{- include "sausage-store-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sausage-store-chart.selectorLabels" . | nindent 4 }}
