---
apiVersion: v1
kind: Service
metadata:
   name: {{ .Values.name }}
   labels:
     app: {{ .Values.name }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: {{ .Values.name }}